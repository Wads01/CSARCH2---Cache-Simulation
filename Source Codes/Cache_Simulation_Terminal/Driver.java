import Controller.CacheSimulator;

/**
 * The Driver class serves as the entry point for the cache simulation program.
 * It initializes a CacheSimulator instance to start the simulation.
 */
public class Driver{
    public static void main(String[] args){
        CacheSimulator cacheSim = new CacheSimulator();
    }
}